Place the original, licensed sale notification sound here:

`new-sale.mp3`

The Village will try to play `/sounds/new-sale.mp3` once for each unique
`new_sale` business event. If the file is missing or the browser blocks audio
until the owner interacts with the page, the app logs a warning and continues.
